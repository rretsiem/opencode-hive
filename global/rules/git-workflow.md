# Git Workflow

- Run the `check` tool after every edit (lint + format + test)
- Commit messages: imperative mood, explain "why" not "what"
- One concern per commit — don't bundle unrelated changes
- Read the diff before committing to catch accidental changes
